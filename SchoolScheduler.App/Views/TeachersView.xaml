<UserControl x:Class="SchoolScheduler.App.Views.TeachersView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="10">
  <Grid.ColumnDefinitions><ColumnDefinition Width="280"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
  <Grid Grid.Column="0" Margin="0,0,12,0">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <DockPanel Margin="0,0,0,8">
    <Button DockPanel.Dock="Right" Content="Найти" Command="{Binding LoadCommand}" Padding="12,4"/>
    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,0"/>
   </DockPanel>
   <DataGrid Grid.Row="1" ItemsSource="{Binding Teachers}" SelectedItem="{Binding SelectedTeacher}"
    AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
    <DataGrid.Columns>
     <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="*"/>
     <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="70"/>
    </DataGrid.Columns>
   </DataGrid>
   <Button Grid.Row="2" Content="Добавить учителя" Command="{Binding AddCommand}" Margin="0,8,0,0" Padding="8"/>
  </Grid>
  <Grid Grid.Column="1">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <StackPanel>
    <TextBlock Text="ФИО"/><TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
    <CheckBox Content="Активен" IsChecked="{Binding IsActive}" Margin="0,0,0,10"/>
    <TextBlock Text="Доступность по дням и урокам" FontWeight="SemiBold"/>
    <TextBlock Text="Отметьте уроки, в которые учитель доступен" Foreground="Gray" Margin="0,2,0,8"/>
   </StackPanel>
   <DataGrid Grid.Row="1" ItemsSource="{Binding Availability}" AutoGenerateColumns="False" CanUserAddRows="False">
    <DataGrid.Columns>
     <DataGridTextColumn Header="День" Binding="{Binding DayName}" IsReadOnly="True" Width="*"/>
     <DataGridCheckBoxColumn Header="0" Binding="{Binding Lesson0}"/><DataGridCheckBoxColumn Header="1" Binding="{Binding Lesson1}"/>
     <DataGridCheckBoxColumn Header="2" Binding="{Binding Lesson2}"/><DataGridCheckBoxColumn Header="3" Binding="{Binding Lesson3}"/>
     <DataGridCheckBoxColumn Header="4" Binding="{Binding Lesson4}"/><DataGridCheckBoxColumn Header="5" Binding="{Binding Lesson5}"/>
     <DataGridCheckBoxColumn Header="6" Binding="{Binding Lesson6}"/><DataGridCheckBoxColumn Header="7" Binding="{Binding Lesson7}"/>
     <DataGridCheckBoxColumn Header="8" Binding="{Binding Lesson8}"/>
    </DataGrid.Columns>
   </DataGrid>
   <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
    <Button Content="В архив" Command="{Binding ArchiveCommand}" Padding="14,6" Margin="0,0,8,0"/>
    <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="14,6"/>
   </StackPanel>
  </Grid>
 </Grid>
</UserControl>
