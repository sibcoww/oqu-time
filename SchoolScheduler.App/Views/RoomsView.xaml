<UserControl x:Class="SchoolScheduler.App.Views.RoomsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid Margin="10"><Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
  <Grid Margin="0,0,12,0"><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <DataGrid ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="Номер / название" Binding="{Binding Name}" Width="*"/><DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="65"/></DataGrid.Columns></DataGrid>
   <Button Grid.Row="1" Content="Добавить кабинет" Command="{Binding AddCommand}" Margin="0,8,0,0" Padding="8"/>
  </Grid>
  <Grid Grid.Column="1"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <StackPanel><TextBlock Text="Номер или название"/><TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/><TextBlock Text="Тип"/><ComboBox ItemsSource="{Binding RoomTypes}" SelectedItem="{Binding Type}" Margin="0,4,0,8"/><CheckBox Content="Активен" IsChecked="{Binding IsActive}" Margin="0,0,0,8"/><TextBlock Text="Доступность" FontWeight="SemiBold"/></StackPanel>
   <DataGrid Grid.Row="1" ItemsSource="{Binding Availability}" AutoGenerateColumns="False" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="День" Binding="{Binding DayName}" IsReadOnly="True" Width="*"/><DataGridCheckBoxColumn Header="0" Binding="{Binding Lesson0}"/><DataGridCheckBoxColumn Header="1" Binding="{Binding Lesson1}"/><DataGridCheckBoxColumn Header="2" Binding="{Binding Lesson2}"/><DataGridCheckBoxColumn Header="3" Binding="{Binding Lesson3}"/><DataGridCheckBoxColumn Header="4" Binding="{Binding Lesson4}"/><DataGridCheckBoxColumn Header="5" Binding="{Binding Lesson5}"/><DataGridCheckBoxColumn Header="6" Binding="{Binding Lesson6}"/><DataGridCheckBoxColumn Header="7" Binding="{Binding Lesson7}"/><DataGridCheckBoxColumn Header="8" Binding="{Binding Lesson8}"/></DataGrid.Columns></DataGrid>
   <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0"><Button Content="В архив" Command="{Binding ArchiveCommand}" Padding="12,6" Margin="0,0,8,0"/><Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="12,6"/></StackPanel>
  </Grid>
 </Grid>
</UserControl>
