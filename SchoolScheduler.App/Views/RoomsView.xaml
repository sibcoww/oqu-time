<UserControl x:Class="SchoolScheduler.App.Views.RoomsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><Grid Margin="10"><Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions><Grid Margin="0,0,12,0"><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom}" AutoGenerateColumns="False" IsReadOnly="True"><DataGrid.Columns><DataGridTextColumn Header="Кабинет" Binding="{Binding Name}" Width="*"/></DataGrid.Columns></DataGrid><Button Grid.Row="1" Content="Добавить" Command="{Binding AddCommand}"/></Grid><Grid Grid.Column="1"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><StackPanel><TextBlock Text="Название"/><TextBox Text="{Binding Name}"/><TextBlock Text="Тип"/><ComboBox ItemsSource="{Binding RoomTypes}" SelectedItem="{Binding Type}"/><CheckBox Content="Активен" IsChecked="{Binding IsActive}"/><TextBlock Text="Доступность по сменам и урокам" FontWeight="Bold" Margin="0,8,0,4"/><DockPanel><ItemsControl DockPanel.Dock="Right" ItemsSource="{Binding DayNames}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" Width="42" TextAlignment="Center"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock Text="Смена / урок"/></DockPanel></StackPanel><ListBox Grid.Row="1" ItemsSource="{Binding Availability}"><ListBox.ItemTemplate><DataTemplate><DockPanel><ItemsControl DockPanel.Dock="Right" ItemsSource="{Binding Cells}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><CheckBox IsChecked="{Binding IsAvailable}" Width="42"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><TextBlock><Run Text="{Binding ShiftName}"/><Run Text=" / "/><Run Text="{Binding LessonNumber}"/></TextBlock></DockPanel></DataTemplate></ListBox.ItemTemplate></ListBox><StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="В архив" Command="{Binding ArchiveCommand}" Margin="0,0,8,0"/><Button Content="Сохранить" Command="{Binding SaveCommand}"/></StackPanel></Grid></Grid></UserControl>
